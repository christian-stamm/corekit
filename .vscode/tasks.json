{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Flash",
            "type": "process",
            "command": "openocd",
            "args": [
                "-f",
                "interface/cmsis-dap.cfg",
                "-f",
                "${env:PICO_OPENOCD_PATH}/tcl/target/rp2350.cfg",
                "-c",
                "adapter speed 5000; program ${command:cmake.getLaunchTargetPath} verify reset exit"
            ],
            "problemMatcher": []
        },
    ]
}